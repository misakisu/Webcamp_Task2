<body>

<% if @book.errors.any? %>
    <h3><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h3>
      <% @book.errors.full_messages.each do |message| %>
        <%= message %>
      <% end %>
<% end %>

<h1 class="space">Editing Book</h1>
<div>
<%= form_for(@book) do |f| %>

    <div>
    <p>Title</p>
    <div class="space"><%= f.text_field :title %></div>

    <p>Body</p>
    <div class="space"><%= f.text_area :body %></div>
    </div>

    <div class="space"><%= f.submit ' Update Book ' %></div>

<% end %>

<p><%= link_to "Show", book_path(@book.id) %> | <%= link_to "Back", books_path %></p>

</body>